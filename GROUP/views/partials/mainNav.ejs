<!-- Nav Bar -->
<nav class="navbar navbar-expand-lg bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Incident Report System</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
      data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" 
      aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

          <li class="nav-item">
            <% if (title == "Home") { %>
              <a class="nav-link active" href="/home">
            <% } else { %>
              <a class="nav-link" href="/home">
                <% } %><i class="fa-solid fa-house"></i> Home</a>
              </li>

              <li class="nav-item">
                <% if (title == "Incident Report") { %>
                  <a class="nav-link active" href="/incident">
                <% } else { %>
                  <a class="nav-link" href="/incident">
                <% } %>
                <i class="fa-solid fa-arrows-to-circle"></i> Incident Report</a>    
              </li>

          <li class="nav-item">
            <% if (title == "About") { %>
              <a class="nav-link active" href="/about">
            <% } else { %>
              <a class="nav-link" href="/about">
            <% } %>
            <i class="fa-solid fa-hourglass-end"></i> About</a>    
          </li>
          
          <li class="nav-item">
            <% if (title == "Login") { %>
              <a class="nav-link active" href="/login">
            <% } else { %>
              <a class="nav-link" href="/login">
                <% } %><i class="fa-solid fa-right-to-bracket"></i> Login</a>
              </li>
              <li class="nav-item">
            <% if (title == "Register") { %>
              <a class="nav-link active" href="/register">
            <% } else { %>
              <a class="nav-link" href="/register">
                <% } %><i class="fa-solid fa-user-plus"></i> Register</a>
              </li>
          </ul>
      </div>
    </div>
  </nav>